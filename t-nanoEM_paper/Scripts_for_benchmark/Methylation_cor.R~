Libraryoutput_File
#  <- Args[2]
Sample1r <- rf(32)

#  <- argstitle <- sprintf("N = %d r = %.3f", nrow(data), c)

# PDF
pdf(output_file, width = 8)
ggplot(data, aes(frequency_1, frequency_2)) +
  geom_bin2d(bins = 25) +
  scale_fill_gradientn(colors = r, trans = "log10") +
  xlab(paste0(sample1_name, " Methylation Rate (%)")) +
  ylab(paste0(sample2_name, " Methylation Rate (%)")) +
  theme_bw(base_size = 20) +
  ggtitle(title)
dev.off()
[3]
sample2_name <- args[4]

#   Stop(")
Library: Rscript your_script_name.R <input_file.tsv> <output_file.pdf> <sample1_name> <sample2_name>", call. = FALSE)
}

# (RColorBrewer)

# 